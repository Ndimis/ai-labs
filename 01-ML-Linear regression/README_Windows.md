# Linear Regression (from scratch)

A minimal implementation of linear regression with gradient descent using only NumPy.
This project uses **synthetic data only** and is intended for learning purposes.

---

## 🔧 Setup

### 🪟 Windows (PowerShell)

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

#### if activation is blocked

Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
